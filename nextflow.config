process {
    withName: FUSIONFS_DEBUG {
        queue = 'nextflow-task-4cpu_32gb'
        cpus = 4
        memory = 30.GB

        publishDir = [
            path: { "${params.outdir}" },
            mode: 'copy'
        ]
    }
}
